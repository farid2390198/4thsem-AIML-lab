Start

1. Import required libraries (NumPy, Pandas, Matplotlib, Sklearn)

2. Load the Iris dataset.

3. Select the required features from the dataset
      Feature 1 → Sepal Length
      Feature 2 → Petal Length

4. Store the selected features in a variable X.

5. Choose the number of clusters
      K ← 3

6. Initialize the K-Means clustering model with K clusters.

7. Fit the K-Means model on dataset X.

8. Assign each data point to the nearest centroid
      Generate cluster labels.

9. Compute the centroids of each cluster.

10. Visualize the clusters
       Plot data points using a scatter plot
       Color points based on cluster labels.

11. Plot the centroids on the graph using a different marker.

12. Display the clustered dataset with cluster labels.

Stop